/*信号(singnal)就是告知某个进程发生了某个时间的通知，又是也称为软件中断。信号通常是异步发生的，也就是说进程预先不知道信号的准确发生时刻。
信号可以：

由一个进程发给另一个进程（或自身）：
由内核发给某个进程
上一节结尾提到SIGCHLD信号就是由内核在任何一个进程终止时发给它的父进程的一个信号
每个信号都有一个与之关联的处置（disposition）也成为行为action.我们通过调用sigaction函数来设定一个信号的处置，并有三种选择。
1我们可以提供一个函数，只要由特定信号发生它就被调用。这样的函数称为信号处理函数，这种行为称为捕获信号。由两个信号不能被捕获，它们是SIGKILL和SIGSTOP。信号处理函数由信号值这个单一的整数参数来调用，且没有返回值，其函数原型因此如下：
void handle(int signo);
对于大多数信号来说，调用sigation函数并指定信号发生时所调用的函数就是捕获信号所需的全部工作，不过我们稍后将看到，SIGIO、SIGPOLL和SIGURG这些个信号还要求捕获它们的进程做些额外的工作。
2我们可以把某个信号的处置设定为SIG_IGN来忽略它。SIGKILL和SIGSTOP这两个信号不能被忽略
3我们可以把某个信号的处置设定为SIG_DFL来启动它的默认处置
